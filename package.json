{
  "name": "portfolio-koonporza",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace web",
    "build": "npm run build --workspace web",
    "start": "npm run start --workspace web",
    "lint": "npm run lint --workspace web",
    "dev:api": "npm run dev --workspace trip-gps-api",
    "build:api": "npm run build --workspace trip-gps-api",
    "lint:api": "npm run lint --workspace trip-gps-api",
    "test:api": "npm run test --workspace trip-gps-api"
  }
}
